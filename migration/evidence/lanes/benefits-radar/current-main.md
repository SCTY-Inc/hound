# benefits-radar: current consumer ref

The exact inspected gc-benefits main ref is
f5673e4f625e138d25cf7e6b3830a0e10a74b7f4 (`Remove benefits direct provider
adapters`). It is the current repair after the abffd89 houndd cutover. The
bounded scan classifies only the `TERMINAL_TOKEN` lane-report placeholder as
consumer-owned terminal status documentation; it reports no acquisition
bypass.
